import React from 'react'

import Menu from "./Menu";
import Banner from "./Banner";

const Header = (props) => (
    <header id="home">
    <Menu />
    <Banner />
  </header>
);

export default Header;
